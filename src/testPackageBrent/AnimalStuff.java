package testPackageBrent;

public interface AnimalStuff 
{
	public void poop(); // assumed to be abstract so no need for abstract keyword
	
	
	// note \\
	// you cannot declare any variable in an interface
	// but, every field and var is automatically static and final
}
